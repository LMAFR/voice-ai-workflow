{
  "_comment": "Map a short voice-friendly alias to a real GitHub repo. Copy this to repos.json (gitignored) and edit. 'format' is optional: name of a skill file under config/skills/<name>.md used to reformat the transcript with Claude before opening the issue. Omit it (or set format_enabled=false) to open the issue with the raw transcript.",
  "format_enabled": false,
  "repos": {
    "studio": {
      "repo": "LMAFR/rrss_studio",
      "labels": ["voice", "from-transcript"],
      "format": "default"
    },
    "dance": {
      "repo": "LMAFR/dance-analyzer",
      "labels": ["voice"],
      "format": "default"
    },
    "sunward": {
      "repo": "LMAFR/sunward",
      "labels": ["voice"]
    }
  }
}
